<% if flash.any? %>
    <section id="flash-messages" class="section">
        <div class="container">
            <ul>
                <% flash.each do |type, message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    </section>
<% end %>
