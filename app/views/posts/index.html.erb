<% provide(:title, "Blog Name") %>
<% provide(:page, "blog") %>

<div class="container">
    <div class="has-text-right">
        <% if logged_in? %>
            <%= render 'posts_navbar' %>
        <% else %>
            <%= link_to("Log in", login_path, class: "button is-link") %>
        <% end %> 
    </div>
</div>

<section class="section">
    <div class="container has-text-centered">
        <h1 class="is-size-2">Blog coming soon</h1>
    </div>
</section>